# foo-rum

🧠 Overview

This is a frontend-only React + TailwindCSS app that implements a simple authentication flow and a social feed interface.
All logic is simulated on the client — no backend or real API calls are used.

The app includes:

Feed Page → Shows all posts and allows publishing new ones

Sign In / Sign Up Pages → Simulated auth forms

Auth Modal Flow → If unauthenticated users interact on the feed, a login modal appears
